# Optional: document env vars for local tooling only.
# The desktop app stores the AssemblyAI key in local SQLite Settings — not in .env.
# Never put real keys in this repo.

# ASSEMBLYAI_API_KEY=  # unused by the Tauri app; configure in Settings UI instead
